import { ImageScrollDirective } from './image-scroll.directive';

describe('ImageScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageScrollDirective();
    expect(directive).toBeTruthy();
  });
});
